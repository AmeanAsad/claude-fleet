# Fleet worker secrets — sourced as env vars on each worker VM.
# Copy to ~/.fleet/secrets.env and fill in values.

# Required: Anthropic API key for Claude Code
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: additional env vars available to Claude Code on the worker
# GITHUB_TOKEN=ghp_...
# DATABASE_URL=postgres://...
